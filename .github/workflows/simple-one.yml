name: learn-github-actions
run-name: ${{ github.actor }} is learning GitHub Actions
on: [push]
jobs:
  containertest:
    runs-on: ubuntu-latest
    container:
      image: ubuntu:18.04
    steps:
      - name: checking
        run: lsb_release -a